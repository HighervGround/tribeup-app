# OAuth Environment Variables for Supabase CLI
# Fill in the actual values and then run the CLI commands below

# Google OAuth Configuration
# Get these from: https://console.developers.google.com/
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Apple OAuth Configuration  
# Get these from: https://developer.apple.com/account/resources/identifiers/list/serviceId
APPLE_CLIENT_ID=your_apple_services_id_here
APPLE_CLIENT_SECRET=your_apple_private_key_here

# CLI Commands to set OAuth credentials:
# 1. Set Google OAuth credentials:
#    supabase secrets set --project-ref alegufnopsminqcokelr GOOGLE_CLIENT_ID=your_actual_google_client_id GOOGLE_CLIENT_SECRET=your_actual_google_secret
#
# 2. Set Apple OAuth credentials:
#    supabase secrets set --project-ref alegufnopsminqcokelr APPLE_CLIENT_ID=your_actual_apple_client_id APPLE_CLIENT_SECRET=your_actual_apple_secret
#
# 3. Or use env file:
#    supabase secrets set --project-ref alegufnopsminqcokelr --env-file .env.oauth

# OAuth Provider Setup Instructions:
# 1. For Google OAuth:
#    - Go to Google Cloud Console (https://console.developers.google.com/)
#    - Create OAuth 2.0 credentials
#    - Add authorized redirect URIs:
#      - https://alegufnopsminqcokelr.supabase.co/auth/v1/callback
#
# 2. For Apple OAuth:
#    - Create Services ID in Apple Developer Console
#    - Configure Sign in with Apple
#    - Add redirect URIs:
#      - https://alegufnopsminqcokelr.supabase.co/auth/v1/callback
#    - Generate private key and convert to proper format
