# ============================================================================
# TribeUp Environment Variables Configuration
# ============================================================================
# 
# SECURITY NOTICE:
# - NEVER commit real secrets to version control
# - Copy this file to .env and fill in real values
# - Add all .env files (except .env.example) to .gitignore
# - Use deployment platform's secrets management for production
#
# For production deployment, set these in your hosting platform:
# - Vercel: Project Settings > Environment Variables
# - Netlify: Site settings > Environment variables
# ============================================================================

# ============================================================================
# REQUIRED VARIABLES (App will not start without these)
# ============================================================================

# Supabase Configuration
# Get from: https://supabase.com/dashboard/project/_/settings/api
# Security: The anon key is safe to expose (enforces RLS policies)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================================================
# RECOMMENDED VARIABLES (Features may be limited without these)
# ============================================================================

# App URL - Required for OAuth redirects and deep linking
# Production: Set to your deployed domain (e.g., https://tribeup.app)
# Development: Leave empty to auto-detect (defaults to http://localhost:5173)
VITE_APP_URL=

# Google Maps API (Required for map features)
# Get from: https://console.cloud.google.com/apis/credentials
# Enable: Maps JavaScript API, Places API, Geocoding API
# Security: Restrict to your domains in API Console
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
VITE_GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# Weather API (Required for weather features)
# Option 1: OpenWeatherMap - https://openweathermap.org/api
VITE_OPENWEATHER_API_KEY=your_openweather_api_key_here
# Option 2: WeatherAPI.com - https://www.weatherapi.com/
VITE_WEATHERAPI_KEY=your_weatherapi_key_here

# Push Notifications
# Generate with: npx web-push generate-vapid-keys
# Only the PUBLIC key goes here; PRIVATE key goes in server-side config
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key_here

# ============================================================================
# OPTIONAL VARIABLES (Have sensible defaults)
# ============================================================================

# App Configuration
VITE_APP_VERSION=1.0.0
VITE_ENVIRONMENT=development
VITE_ENABLE_MOCK_DATA=false

# Weather Settings
VITE_WEATHER_TEMP_UNIT=fahrenheit
VITE_WEATHER_UPDATE_INTERVAL=3600
VITE_WEATHER_INCLUDE_HOURLY=true
VITE_WEATHER_RAIN_THRESHOLD=0.1
VITE_WEATHER_WIND_THRESHOLD=15

# Timeout Configuration (milliseconds)
VITE_PROFILE_CHECK_TIMEOUT=5000
VITE_PROFILE_FORCE_TIMEOUT=15000
VITE_PRESENCE_HEARTBEAT_INTERVAL=30000
VITE_LOCATION_MONITORING_INTERVAL=1800000

# Business Rules
VITE_GAME_EDIT_RESTRICTION_HOURS=2
VITE_GAME_DELETION_RESTRICTION_HOURS=4

# Logging
# Options: debug, info, warn, error
VITE_LOG_LEVEL=info

# ============================================================================
# SERVER-SIDE ONLY (Set in Supabase Dashboard, NOT in client code)
# ============================================================================
# These are for reference only - NEVER expose these in client-side code
#
# Supabase Service Role Key (admin access - bypasses RLS)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
#
# OAuth Provider Secrets (set in Supabase Auth settings)
# SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=your_google_client_id
# SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=your_google_client_secret
# SUPABASE_AUTH_EXTERNAL_APPLE_CLIENT_ID=your_apple_client_id
# SUPABASE_AUTH_EXTERNAL_APPLE_SECRET=your_apple_client_secret
#
# VAPID Private Key (for server-side push notifications)
# VAPID_PRIVATE_KEY=your_vapid_private_key_here
# ============================================================================
